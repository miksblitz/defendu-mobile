# Optional: API base URL (forgot password, reset). Default: https://defendu-mobile.vercel.app
# EXPO_PUBLIC_API_BASE_URL=https://defendu-mobile.vercel.app

# Pose reference is now set by the training pipeline (reference videos + payment, 1–2 days).
# The app no longer calls an extraction service on publish. EXPO_PUBLIC_POSE_EXTRACTION_URL is unused.
