/**
 * Temporary product switches. Flip back before production when the flow should be enforced again.
 */

/** When true, new accounts are created immediately with no email OTP step. */
export const SKIP_REGISTRATION_EMAIL_VERIFICATION = true;
